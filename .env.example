# Copy to .env and fill in values: cp .env.example .env

# Required for agents and demo (Anthropic)
ANTHROPIC_API_KEY=

# SQLAlchemy async URL (default: local SQLite)
DATABASE_URL=sqlite+aiosqlite:///./genie.db

# Model id passed to the Anthropic API
CLAUDE_MODEL=claude-sonnet-4-6

# Max tool-use loop iterations per agent run
MAX_AGENT_ITERATIONS=10

# Python logging level (e.g. DEBUG, INFO, WARNING)
LOG_LEVEL=INFO
